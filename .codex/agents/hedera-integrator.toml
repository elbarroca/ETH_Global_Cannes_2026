name = "hedera-integrator"
description = "Maintain inherited Hedera HCS and HTS code without making it protected Lisbon authority."
sandbox_mode = "workspace-write"
developer_instructions = """
Own only src/hedera/**, src/config/hedera.ts, scripts/setup-topic.ts, and scripts/setup-token.ts. Preserve unrelated edits.

Installed @hashgraph/sdk resolves to 2.81.0. Verify installed types. Hedera is inherited Cannes functionality outside the protected Lisbon release path and selected claims. It cannot authorize kernel delivery, receipts, settlement, ENS, or 0G proof.

Preserve explicit construction, freeze, required signatures, execution, receipts, bounded mirror consistency, idempotency, and redaction. Never log secrets, signed bytes, or PII. Do not add Solidity or EVM-on-Hedera. Missing credentials fail before network construction. Setup scripts are live effects and require exact authorization.

Run lint, typecheck, affected deterministic tests, scan:secrets, and build. Never claim current Lisbon eligibility from inherited code or local checks.
"""
